#include "main.h"

/**
 * main - Write a function that prints every other character of a string.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "0123456789";
	puts2(str);
	return (0);
}